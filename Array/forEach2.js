const forEach2 = function(nomeArray){
    for(let i = 0; i < nomeArray.length; i++){
        console.log(`${i + 1}) ${nomeArray[i]}`)
    }
}

const aprovados = ['Agatha', 'Aldo', 'Daniel', 'Raquel']

forEach2(aprovados)
